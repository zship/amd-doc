h2
    span.moduleName=module
    -if (cl.extends)
        span.extends=' extends '
            each base, i in cl.extends
                a.superclass(href=base.link, title=base.longName)=base.name
                - if (i < cl.extends.length - 1)
                    span.sep=','

- if (cl.module && cl.module.description)
    div.overview
        h3
            a(href='#/' + module + '#SOverview', rel=module + '#SOverview') Overview

- if (cl.module && cl.module.meta && cl.module.meta.dependencies && cl.module.meta.dependencies.length)
    div.dependencies
        h3
            a(href='#/' + module + '#SDependencies', rel=module + '#SDependencies') Dependencies

- if (cl.constructor && cl.constructor.kind === 'class')
    div.constructor
        h3
            a(href='#/' + module + '#SConstructor', rel=module + '#SConstructor') Constructor

- if (cl.properties && Object.keys(cl.properties).length)
    div.properties
        h3
            a(href='#/' + module + '#SProperties', rel=module + '#SProperties') Properties
        ul
            each key in Object.keys(cl.properties).sort()
                - var prop = cl.properties[key]
                - var clazz = (prop.inherited || prop.overridden) ? 'inherited property' : 'property'
                li(class=clazz)
                    a.name(href='#/' + prop.longname, rel=prop.longname)=prop.name
                    span=' : '
                    span.returns
                        each type, j in prop.types
                            span.returnType=type.name
                            - if (j < prop.types.length - 1)
                                span.or='|'
                    &nbsp;

- if (cl.methods && Object.keys(cl.methods).length)
    div.methods
        h3
            a(href='#/' + module + '#SMethods', rel=module + '#SMethods') Methods
        ul
            each key in Object.keys(cl.methods).sort()
                - var method = cl.methods[key]
                - var clazz = (method.inherited || method.overridden) ? 'inherited method' : 'method'
                li(class=clazz)
                    a.name(href='#/' + method.longname, rel=method.longname)=method.name
                    span=' : '
                    span.returns
                        each ret in method.returns
                            each type, j in ret.types
                                span.returnType=type.name
                                - if (j < ret.types.length - 1)
                                    span.or='|'
                            &nbsp;

- if (cl.events && Object.keys(cl.events).length)
    div.events
        h3
            a(href='#/' + module + '#Sevents', rel=module + '#Sevents') Events
        ul
            each key in Object.keys(cl.events).sort()
                - var ev = cl.events[key]
                - var clazz = (ev.inherited || ev.overridden) ? 'inherited event' : 'event'
                li(class=clazz)
                    a.name(href='#/' + ev.longname, rel=ev.longname)=ev.name
